# Changelog

## 1.0.0
- Coletor DR Série I via RSS com normalizações e persistência em SQLite.
- Pipeline estável (collect -> CollectResult) + testes offline/smoke.
- Export LLM-ready (JSON + TXT) com separação `raw/` vs `norm/` (UTF-8).
- CLI (`src.leis`) para listagem e estatísticas por tipo.
- Build PyInstaller + launcher Windows + scripts utilitários.
